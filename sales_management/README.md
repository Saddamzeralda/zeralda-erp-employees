# نظام إدارة المبيعات - Sales Management System
## Zeralda ERP Tools

نظام متكامل لإدارة المبيعات مع ميزات متقدمة للتقارير، التنبيهات، والمدفوعات.

---

## 🌟 المميزات الرئيسية

### 1️⃣ لوحة تحكم ديناميكية
- **6+ مؤشرات أداء رئيسية (KPIs)**:
  - إجمالي المبيعات مع نسبة النمو الشهري
  - عدد الفواتير (مدفوعة / غير مدفوعة)
  - المبالغ المستحقة
  - الفواتير المتأخرة
  - إجمالي العملاء مع العملاء الجدد
  - متوسط قيمة الفاتورة

- **رسوم بيانية تفاعلية**:
  - رسم بياني خطي لنمو المبيعات (آخر 6 أشهر)
  - رسم دائري لتوزيع طرق الدفع
  - رسم بياني عمودي للإيرادات الشهرية (آخر 12 شهر)
  - رسم دائري لحالة الفواتير

### 2️⃣ تقارير متقدمة (PDF، إحصائيات)
- **تقرير الفاتورة الفردي**: إنشاء PDF احترافي لكل فاتورة
- **التقرير الشهري**: تقرير شامل بالمبيعات والإيرادات حسب الشهر
- **تقرير الفواتير المستحقة (Aging Report)**: 
  - تصنيف الفواتير المتأخرة حسب الفترات: 1-30، 31-60، 61-90، +90 يوم
- **تقرير المدفوعات**: تقرير مفصل بجميع المدفوعات خلال فترة محددة
- **التصدير**: إمكانية تصدير البيانات بصيغ Excel و CSV

### 3️⃣ بحث وفلترة متقدمة
- **بحث سريع** مع Debouncing لتحسين الأداء
- **فلاتر متعددة**:
  - نطاق التاريخ (من - إلى)
  - حالة الفاتورة (مدفوعة، غير مدفوعة، جزئية)
  - طريقة الدفع
  - العميل
  - نطاق السعر (قابل للإضافة)
- **حفظ معايير البحث**: حفظ الفلاتر المفضلة لإعادة استخدامها
- **فرز ديناميكي**: ترتيب النتائج حسب مختلف الأعمدة

### 4️⃣ إدارة الفواتير المستحقة (Reminders)
- **تتبع تلقائي**: مراقبة الفواتير المتأخرة والقادمة
- **نظام تنبيهات ذكي**:
  - تذكيرات قبل الاستحقاق (7، 3، 1 أيام)
  - تذكيرات بعد الاستحقاق (1، 7، 14، 30 يوم)
- **إرسال يدوي أو تلقائي**: خيار التحكم بإرسال التنبيهات
- **سجل التذكيرات**: متابعة جميع التذكيرات المرسلة
- **دعم متعدد القنوات**: بريد إلكتروني، WhatsApp (قابل للتفعيل)

### 5️⃣ دعم طرق دفع متعددة
- **طرق الدفع المدعومة**:
  - نقداً (Cash)
  - تحويل بنكي (Bank Transfer)
  - Stripe (جاهز للتكامل)
  - PayPal (جاهز للتكامل)
- **تتبع المدفوعات**: سجل كامل لجميع المعاملات
- **حالة الدفع**: مدفوعة، غير مدفوعة، مدفوعة جزئياً
- **تقارير الدفع**: إحصائيات تفصيلية حسب طريقة الدفع

---

## 📦 التقنيات المستخدمة

### المكتبات الخارجية
- **Chart.js** - الرسوم البيانية التفاعلية
- **jsPDF & html2pdf** - إنشاء ملفات PDF
- **SheetJS (xlsx)** - تصدير Excel/CSV
- **Font Awesome** - الأيقونات
- **Moment.js** (اختياري) - معالجة التواريخ

### البنية التقنية
- **Vanilla JavaScript** - بدون اعتماد على إطار عمل
- **CSS3** مع متغيرات CSS للتخصيص السهل
- **LocalStorage** - تخزين البيانات محلياً
- **Responsive Design** - تصميم متجاوب لجميع الأجهزة

---

## 🏗️ البنية التنظيمية

```
sales_management/
├── index.html                    # الواجهة الرئيسية
├── config.js                     # إعدادات التطبيق
├── css/
│   ├── style.css                 # التصاميم الأساسية
│   └── dashboard.css             # تصاميم لوحة التحكم
├── js/
│   ├── app.js                    # التطبيق الرئيسي وإدارة البيانات
│   ├── dashboard.js              # لوحة التحكم والمؤشرات
│   ├── reports.js                # نظام التقارير وPDF
│   ├── search-filter.js          # البحث والفلترة المتقدمة
│   ├── reminders.js              # نظام التنبيهات
│   └── payments.js               # نظام الدفع
└── assets/
    └── templates/
        └── invoice-template.html # قالب الفاتورة
```

---

## 🚀 كيفية الاستخدام

### 1. التثبيت
```bash
# استنساخ المستودع
git clone https://github.com/Saddamzeralda/zeralda-erp-employees.git

# الانتقال إلى مجلد المشروع
cd zeralda-erp-employees/sales_management
```

### 2. التشغيل
- افتح `index.html` مباشرة في المتصفح
- أو استخدم خادم محلي:
```bash
# باستخدام Python
python -m http.server 8000

# باستخدام Node.js
npx http-server
```

### 3. الإعداد الأولي
- افتح `config.js` لتخصيص الإعدادات:
  - مفاتيح API لـ Stripe و PayPal
  - إعدادات البريد الإلكتروني و WhatsApp
  - العملة الافتراضية
  - فترات التذكيرات

---

## 📊 الوحدات الرئيسية

### 1. وحدة إدارة البيانات (`app.js`)
```javascript
// إنشاء فاتورة جديدة
const result = app.addInvoice({
    customer: customerData,
    items: [{ description: 'منتج 1', quantity: 2, price: 1000 }],
    paymentMethod: 'cash'
});

// تحديث فاتورة
app.updateInvoice(invoiceId, { paymentStatus: 'paid' });

// حذف فاتورة
app.deleteInvoice(invoiceId);
```

### 2. وحدة لوحة التحكم (`dashboard.js`)
```javascript
// إنشاء لوحة التحكم
const dashboard = new Dashboard(app);
dashboard.init();

// تحديث البيانات
dashboard.refresh();
```

### 3. وحدة التقارير (`reports.js`)
```javascript
// إنشاء تقرير فاتورة
await reportsManager.generateInvoicePDF(invoiceId);

// تقرير شهري
await reportsManager.generateMonthlySalesReport(2024, 1);

// تقرير الاستحقاق
await reportsManager.generateAgingReport();

// تصدير Excel
reportsManager.exportToExcel();
```

### 4. وحدة البحث والفلترة (`search-filter.js`)
```javascript
// تطبيق الفلاتر
searchFilter.applyFilters();

// حفظ بحث
searchFilter.saveSearch('بحثي المفضل');

// تطبيق بحث محفوظ
searchFilter.applySavedSearch(0);
```

### 5. وحدة التنبيهات (`reminders.js`)
```javascript
// إرسال تذكير يدوي
await remindersManager.sendReminder(invoiceId, 'manual');

// الحصول على الفواتير المتأخرة
const overdueInvoices = remindersManager.getOverdueInvoices();
```

### 6. وحدة المدفوعات (`payments.js`)
```javascript
// معالجة دفعة
await paymentsManager.processPayment(invoiceId, 'cash', amount);

// الحصول على سجل المدفوعات
const history = paymentsManager.getPaymentHistory({
    startDate: '2024-01-01',
    method: 'stripe'
});
```

---

## ⚙️ الإعدادات (`config.js`)

### إعدادات الدفع
```javascript
payments: {
    stripe: {
        publishableKey: 'pk_test_YOUR_KEY',
        enabled: true
    },
    paypal: {
        clientId: 'YOUR_CLIENT_ID',
        enabled: true,
        mode: 'sandbox'
    }
}
```

### إعدادات التنبيهات
```javascript
reminders: {
    daysBeforeDue: [7, 3, 1],
    daysAfterDue: [1, 7, 14, 30],
    autoSend: false
}
```

---

## 🎨 التخصيص

### الألوان
يمكن تغيير الألوان في `css/style.css`:
```css
:root {
    --primary-color: #2563eb;
    --secondary-color: #1e40af;
    --success-color: #10b981;
    --warning-color: #f59e0b;
    --danger-color: #ef4444;
}
```

### ألوان الرسوم البيانية
في `config.js`:
```javascript
dashboard: {
    chartColors: [
        '#2563eb', '#10b981', '#f59e0b', 
        '#ef4444', '#8b5cf6', '#06b6d4'
    ]
}
```

---

## 🔒 الأمان

- **لا يتم تخزين بيانات حساسة**: جميع البيانات محلية في المتصفح
- **مفاتيح API**: يجب تأمينها على الخادم في بيئة الإنتاج
- **التحقق من المدخلات**: جميع المدخلات يتم التحقق منها قبل المعالجة
- **معالجة الأخطاء**: Try/catch شامل في جميع الوحدات

---

## 📱 الاستجابة

النظام متجاوب بالكامل ويعمل على:
- ✅ أجهزة الكمبيوتر المكتبية
- ✅ الأجهزة اللوحية
- ✅ الهواتف الذكية

---

## 🌐 دعم اللغات

- العربية (افتراضي - RTL)
- الإنجليزية
- الفرنسية

---

## ✅ معايير النجاح المحققة

- ✔️ **5+ تقارير مختلفة**: فاتورة، شهري، استحقاق، مدفوعات، Excel/CSV
- ✔️ **بحث وفلترة متقدم**: 6 فلاتر مختلفة + حفظ البحث
- ✔️ **نظام تنبيهات تلقائي**: تنبيهات قبل وبعد الاستحقاق
- ✔️ **دعم Stripe و PayPal**: واجهة UI كاملة + جاهز للتكامل
- ✔️ **لوحة تحكم بـ 6+ مؤشرات**: KPIs + 4 رسوم بيانية
- ✔️ **توثيق كامل**: تعليقات شاملة في الكود

---

## 🔄 التحديثات المستقبلية

- [ ] تكامل كامل مع Stripe API
- [ ] تكامل كامل مع PayPal SDK
- [ ] إرسال بريد إلكتروني حقيقي
- [ ] تكامل WhatsApp API
- [ ] مزامنة سحابية
- [ ] تطبيق جوال
- [ ] تقارير إضافية (تحليل العملاء، توقعات المبيعات)

---

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى فتح Issue على GitHub.

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

## 👨‍💻 المطور

تطوير: Zeralda ERP Team

---

**ملاحظة**: هذا النظام يعمل بالكامل في المتصفح باستخدام localStorage. للاستخدام في الإنتاج، يُنصح بتكامله مع backend API لتخزين البيانات بشكل آمن ومركزي.
